version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

python:
  install:
    - method: pip
      path: .
    - requirements: requirements.txt
    - method: pip
      packages:
        - sphinx_rtd_theme

sphinx:
  configuration: docs/conf.py
